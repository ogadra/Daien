pre-commit:
  parallel: false
  commands:
    001_biome_format:
      glob: "*.{js,ts,jsx,tsx,json,jsonc}"
      run: pnpm format {staged_files}
    002_biome_check:
      glob: "*.{js,ts,jsx,tsx,json,jsonc}"
      run: pnpm check {staged_files}
